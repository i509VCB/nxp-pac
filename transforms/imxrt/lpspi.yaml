# Boolean enum transforms for LPSPI peripheral
# Converts single-bit enums with _0/_1 patterns to proper boolean types

transforms:
  - !DeleteEnums
    from: lpspi::vals::(Dozen|Rxmsk|Txmsk)
    bit_size: 1