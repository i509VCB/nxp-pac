# Boolean enum transforms for TRNG peripheral
# Converts single-bit enums with _0/_1 patterns to proper boolean types

transforms:
  - !DeleteEnums
    from: trng::vals::(IntMaskEntVal|IntMaskFrqCtFail|IntMaskHwErr)
    bit_size: 1