# Boolean enum transforms for PMU peripheral
# Converts single-bit enums with _0/_1 patterns to proper boolean types

transforms:
  # Delete Misc0 register control enums
  - !DeleteEnums
    from: pmu::vals::Misc0(Clr|Set|Tog)?(ClkgateCtrl|DisconHighSnvs|ReftopSelfbiasoff)
    bit_size: 1

  # Delete Reg1p1 control enums
  - !DeleteEnums
    from: pmu::vals::Reg1p1(Clr|Set|Tog)?SelrefWeakLinreg
    bit_size: 1